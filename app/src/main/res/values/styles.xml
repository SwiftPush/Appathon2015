<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">#FF5722</item>
        <item name="android:colorPrimaryDark">#E64A19</item>
        <!--<item name="android:colorAccent">@color/accent</item>!-->
    </style>
    <style name="keyboard_style">
        <item name="android:keyBackground">@drawable/key</item>
        <item name="android:keyTextColor">#22343D</item>
        <item name="android:keyTextSize">25dp</item>
        <item name="android:shadowRadius">0</item>
        <item name="android:background">#e2e5e6</item>
        <item name="android:keyPreviewLayout">@layout/preview</item>
        <item name="android:keyPreviewHeight">120dp</item>
    </style>

</resources>
